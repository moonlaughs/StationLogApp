<Page
    x:Class="StationLogApp.View.CreateTaskPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StationLogApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:StationLogApp.ViewModel"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="ms-appx:///Assets/2.jpg"/>
        </Grid.Background>
        
        <Grid.DataContext>
            <viewModel:CrudVM/>
        </Grid.DataContext>
    
            <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Row="1" Grid.Column="1">
            <TextBlock Text="Task description" FontSize="20" Foreground="White" />
            <TextBox x:Name="TaskDescription" Text="{Binding NewItem.TaskName, Mode=TwoWay}" FontSize="20" Foreground="White" Width="200" Height="30" Margin="0,0,700,0"/>
            <TextBlock Text="Schedule" FontSize="20" Foreground="White"/>
            <TextBox Name="Schedule" FontSize="20" Text="{Binding NewItem.TaskSchedule, Mode=TwoWay}" Foreground="White" Width="200" Height="30" Margin="0,0,700,0"/>
            <TextBlock Text="Task type" Foreground="White" FontSize="20"/>
            <ComboBox Name="Tasktype" Foreground="White" FontSize="20" Width="200" Height="30" Margin="0,0,700,0">
            <ComboBoxItem Content="Registration" Foreground="White"/>
            <ComboBoxItem Content="Check" Foreground="White"/>
            </ComboBox>
            <TextBlock Text="Due date:" Foreground="White" FontSize="20"/>
            <DatePicker Width="200" Height="30" Date="{Binding NewItem.DueDate, Mode=TwoWay}"/>
            <TextBlock Text="Equipment name" Foreground="White" FontSize="20"/>
            <TextBox Name="Equipmentname" Text="{Binding NewItem.EquipmentName, Mode=TwoWay}" Foreground="White" FontSize="20" Width="200" Height="30" Margin="0,0,700,0"/>
            <TextBlock Text="Station name" Foreground="White" FontSize="20"/>
            <TextBox Name="Stationname" Text="{Binding NewItem.StationName, Mode=TwoWay}" Foreground="White" FontSize="20" Width="200" Height="30" Margin="0,0,700,0"/>

            <Button Content="CREATE" Command="{Binding DoCreateTask}" FontSize="20" Foreground="White" BorderBrush="White" BorderThickness="2" Margin="0,50,0,0"/>
        </StackPanel>

    </Grid>
</Page>
