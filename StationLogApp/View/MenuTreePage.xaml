<Page
    x:Class="StationLogApp.View.MenuTreePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StationLogApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:StationLogApp.ViewModel"
    mc:Ignorable="d">

    <Grid>
        <!--<Grid.DataContext>
            <viewModel:LoginVM/>
        </Grid.DataContext>-->
        <Grid.Background>
            <ImageBrush ImageSource="../Assets/2.jpg" Stretch="Fill"/>
        </Grid.Background>

        <!--<Grid.DataContext>
            <viewModel:ButtonsVm/>
        </Grid.DataContext>-->

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal" Height="798" Margin="0,154,-1,48">
            <StackPanel.Background>
                <ImageBrush ImageSource="ms-appx:///Assets/sign.png" Stretch="Uniform"/>
            </StackPanel.Background>
            <Button Content="TASKS" FontSize="100" Foreground="White" Background="#00000000" BorderBrush="#00000000" Height="300" Width="750" Command="{Binding GoTask}"/>
            <Button Content="HISTORY" FontSize="100" Foreground="White" Background="#00000000" BorderBrush="#00000000" Height="300" Width="750" Command="{Binding GoDone}"/>
        </StackPanel>
    </Grid>
</Page>
