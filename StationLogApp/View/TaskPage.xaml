<Page
    x:Class="StationLogApp.View.TaskPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StationLogApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:StationLogApp.ViewModel"
    mc:Ignorable="d">
    <Grid>
        <Grid Background="#FF232121">
            <Grid.DataContext>
                <viewModel:TaskVm/>
            </Grid.DataContext>
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="5">
               <TextBlock Text="Filters" Foreground="White" FontSize="20" Margin="0,0,10,0"/>
               <ComboBox Name="Stations" Foreground="White" Margin="20,0,0,0" Width="200">
                <ComboBoxItem Content="Sation1"  Foreground="White"/>
                <ComboBoxItem Content="Station2" Foreground="White"/>
                <ComboBoxItem Content="Station3" Foreground="White"/>
               </ComboBox>
                <ComboBox Name="Date" Margin="20,0,0,0" Width="200">
                <ComboBoxItem Content="Today" Foreground="White"/>
                <ComboBoxItem Content="This week" Foreground="White"/>
                <ComboBoxItem Content="This month" Foreground="White"/>
               </ComboBox>
            </StackPanel>

            <ListView Name="Tasks" ItemsSource="{Binding TaskCatalog}" Grid.Row="3" Grid.Column="1">
                <ListView.Background>Transparent</ListView.Background>
                <StackPanel Orientation="Horizontal">
                    <StackPanel>
                        <TextBlock Text="{Binding }" Foreground="White" FontSize="18"/>
                        <TextBlock Text="Location:" Foreground="White" FontSize="18"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Date:" Foreground="White" FontSize="18"/>
                        <TextBlock Text="Due To:" Foreground="White" FontSize="18"/>
                    </StackPanel>
                    <TextBlock Text="Add comment" Foreground="White" FontSize="18"/>
                    <TextBox PlaceholderText="Add comment" Foreground="White" FontSize="18" Height="29" Width="243"/>
                    <Button Content="Save" Foreground="White"/>
                    <Line Fill="White" Stroke="White" Height="0.5" StrokeThickness="2" X1="1000"/>
                </StackPanel>
            </ListView>


            <ListView Grid.Row="3" Grid.Column="2" Name="Notes">
                <ListView.Background>Transparent</ListView.Background>
                <StackPanel>
                    <TextBlock Text="Name:" Foreground="White" FontSize="18"/>
                    <TextBlock Text="Due date:" Foreground="White" FontSize="18"/>
                    <TextBlock Text="Description:" Foreground="White" FontSize="18"/>
                    <TextBlock Text="Technician/Station:" Foreground="White" FontSize="18"/>
                    <Button Content="Add log" Foreground="White" FontSize="18"/>
                    <Line Fill="White" Stroke="White" Height="0.5" StrokeThickness="2" X1="1000"/>
                </StackPanel>
            </ListView>
        </Grid>
    </Grid>
</Page>
